## 1. 프로젝트 개요

- **프로젝트 명**: SBC 365 (소상공인 상권 분석 및 지원 서비스)
- **프로젝트 주제**: 소상공인이 실제로 “어디에, 어떤 업종으로, 어떤 지원금을 활용해” 창업·이전을 할지까지 연결해 주는 **데이터 기반 상권 분석 & 지원사업 매칭 플랫폼**
- **기술 스택**
  - **Frontend**: React, TypeScript, React Router, Zustand, Tailwind CSS, Kakao Maps JavaScript API, React Markdown
  - **Backend**: Node.js(Express), MySQL, Axios
  - **데이터 수집·분석 외부 API**
    - 국토부 RTMS 실거래 API
    - VWorld 지가(개별공시지가) WFS API
    - SGIS 인구 통계 API
    - 공공데이터포털(ODCloud) 상권 데이터
    - Bizinfo 소상공인 지원사업 API
  - **AI & 문서 처리**
    - OpenAI Chat Completion API (gpt-4o)
    - Python 스크립트 `hwp_hwpx_pdf_extract_text.py` (HWP/HWPX/PDF 텍스트 추출)
    - (설계 상) Playwright 기반 크롤러를 통해 지원사업 공고 페이지/첨부파일 자동 수집

---

## 2. 프로젝트 목적 및 차별성

### 2-1. 기존 ‘소상공인 365’ 서비스의 한계

- **정보 나열 중심**
  - 정부/지자체 지원사업, 상권 통계, 점포 정보 등 **원자료를 그대로 보여주는 수준**에 머무름
  - 사용자는 “어떤 지원사업이 나에게 맞는지, 어떤 상권이 좋은지”를 스스로 해석해야 함
- **행동으로 이어지지 않는 분석**
  - 상권 통계/인구 통계는 제공되지만,  
    **“그래서 이 지역에 이 업종을 해도 되는지?”**에 대한 **결론·전략 제안이 부족**
  - 개별 지원사업 공고(HWP, PDF)는 **직접 열어 읽어야** 하므로 진입 장벽이 큼

### 2-2. SBC 365의 목표

- **“데이터 → 인사이트 → 행동”까지 연결되는 의사결정 도구**
  - 단순 조회 서비스가 아니라,  
    **“어디에 / 어떤 업종으로 / 어떤 지원금을 활용해 입지할지”**를 제안하는 **실행형 컨설턴트**를 목표로 함.
- **복잡한 공공 데이터/공고문을 한 번에 엮어서 보여주기**
  - 상권 데이터(공시지가, 실거래, 인구, 상가 분포, 폐업/신규 상가)를 합쳐  
    **“상권의 건강 상태와 위험도”**를 한 문장으로 요약.

### 2-3. 핵심 차별 포인트

- **① 공고문 자동 분석**
  - Bizinfo API로 불러온 소상공인 지원사업의 **HWP / HWPX / PDF 첨부파일을 자동으로 변환**.
  - Python + OpenAI를 이용해 **지원 대상, 지원 내용, 금액, 필수 요건**을 **한글 요약으로 즉시 제공**.
  - 사용자는 공고문 원본을 열어보지 않고도 **“나에게 맞는지”**를 빠르게 판단 가능.

- **② 사용자 맞춤형 AI 상권 추천**
  - 사용자가 지도에서 **직접 위치·반경·업종**을 정하면,
    - 공시지가 / 실거래가 / 상가 밀집도 / 폐업·신규 상가 / 인구 통계 / 주요 상권 겹침 정도를 수집·가공하고
    - OpenAI를 이용해서 **마크다운 리포트 형태의 “입지 진단 + 전략 제안”**을 자동으로 생성.
  - 같은 데이터라도 **“어디를 중심으로 어떤 반경을 선택했는지, 어떤 업종인지”**에 따라  
    리포트 내용이 달라지는 **진짜 개인화 상권 분석**.

- **③ 분석 ↔ 추천 페이지 간 데이터 연속성**
  - Zustand 전역 스토어(`useAnalysisStore`)를 사용해
    - 분석 페이지에서 수집·요약한 **모든 정량 데이터(블록별 신규/폐업, 공시지가, 인근 상가, 실거래 요약, 인구 등)**를
    - 추천 페이지로 그대로 전달.
  - 사용자는 **분석 페이지에서 “상권 이해” → 추천 페이지에서 “후보 지역 비교/선택”**으로  
    **끊김 없이 이동**할 수 있음.

---

## 3. 주요 기능 (상세 기술 관점)

### 3-1. 지능형 공고 분석: HWP/HWPX/PDF 자동 요약 파이프라인

#### (1) 지원사업 목록 수집 – `/api/get-support`

- **Backend – Bizinfo API 연동 (`server.js`)**
  - 엔드포인트: `GET /api/get-support?region={지역명}&category=소상공인`
  - 동작
    - `https://www.bizinfo.go.kr/uss/rss/bizinfoApi.do` 를 호출해 최대 500건의 지원사업을 JSON으로 조회.
    - 응답 필드를 가공해 **우리 서비스에서 쓰기 좋은 도메인 모델**로 변환:
      - `id`: 공고 ID (`pblancId`)
      - `title`: 공고 제목
      - `agency`: 주관 기관명
      - `summary`: HTML 제거·공백 정리된 간단 설명
      - `period`: 신청 기간
      - `url`: 공고 상세 페이지 URL
      - `type`: 지원 유형
      - `fileUrl`: 첨부 공고문 파일 경로
      - `fileName`: 첨부 파일 이름
      - `meth`: 신청 방법 요약
    - **지역·대상 필터링**
      - 요청 파라미터 `region`(예: `부산`) 과 공고의 `jrsdInsttNm`(주관 기관명)을 비교해,
        - `기관명이 해당 지역으로 시작`하거나 `중소` 등 중앙부처일 때만 통과.
      - `trgetNm` 에 `소상공인`이 포함된 공고만 남김 → **“해당 지역 소상공인 대상 공고”** 리스트로 축약.

- **Frontend – `AnalysisDashboard` + `SupportPrograms`**
  - `AnalysisPage`에서 분석이 끝난 뒤 `fetchSupportPrograms()` 호출
    - 현재 지도 중심 주소에서 **지역명(예: “부산”)을 추출**해 `/api/get-support` 호출.
  - `SupportPrograms` 컴포넌트는 응답을 받아 **카드 형태의 지원사업 목록** 렌더링:
    - 기본 텍스트 요약(`summary`)은 Bizinfo의 요약 필드를 정제해서 그대로 노출.
    - 사용자가 `AI로 공고문 요약하기` 버튼을 누르면, **첨부파일 단위의 정밀 요약**을 트리거.

#### (2) 첨부 공고문 파일 텍스트 추출 – `hwp_hwpx_pdf_extract_text.py`

- **문제점**
  - 실제 공고문의 본문은 HWP/HWPX/PDF 첨부파일에 들어있어 **단순 HTML 크롤링으로는 내용 파악이 불가능**.
  - 특히 HWP/HWPX는 한글 오피스 전용 포맷으로, 일반 텍스트 변환이 까다로움.

- **Python 스크립트 구조 (`hwp_hwpx_pdf_extract_text.py`)**
  - **HWPX 처리**
    - HWPX는 내부적으로 **Zip 압축 + XML** 구조.
    - `zipfile`로 압축을 열어 `Contents/section*` 파일들을 순회.
    - 각 섹션을 `xml.etree.ElementTree`로 파싱해,
      - 네임스페이스가 `{http://www.hancom.co.kr/hwpml/2011/paragraph}t` 인 `<hp:t>` 태그를 모두 찾고
      - 실제 문단 텍스트(`t.text`)를 리스트에 저장 후 `\n` 기준으로 합침.
  - **PDF 처리 (PyMuPDF)**
    - `fitz.open(file_path)` 로 문서를 열고,
    - 각 페이지에 대해 `page.get_text()` 호출 → 전 페이지 텍스트를 하나의 문자열로 병합.
  - **HWP 처리**
    - `.hwp` 확장자는 Python 라이브러리보다 **검증된 CLI 도구 `hwp5txt`** 사용.
    - `os.system("hwp5txt \"파일경로\"")` 로 외부 커맨드를 호출해 표준 출력을 통해 텍스트를 확보.

```python
def main():
    file_path = sys.argv[1]
    ext = os.path.splitext(file_path)[1].lower()

    if ext == '.hwp':
        os.system(f"hwp5txt \"{file_path}\"")
    elif ext == '.hwpx':
        print(extract_hwpx_text(file_path))
    elif ext == '.pdf':
        print(extract_pdf_text(file_path))
```

- **특징**
  - 파일 확장자만으로 **HWP / HWPX / PDF를 자동 분기** 처리.
  - HWPX는 구조를 직접 파고들어 필요한 태그만 읽어,  
    **스타일·레이아웃과 무관하게 순수 텍스트만 추출**.

#### (3) 공고문 AI 요약 – `/api/support/summarize`

- **Backend 파이프라인 (`server.js`)**
  - 엔드포인트: `POST /api/support/summarize`
  - 입력: `{ id, fileUrl, fileName }`
  - 처리 단계
    1. **첨부 파일 다운로드**
       - Bizinfo에서 내려준 `fileUrl`을 통해 공고문 파일을 임시 디렉터리에 저장.
    2. **Python 스크립트 호출**
       - Node.js에서 `child_process` 또는 Python 런타임을 통해  
         `hwp_hwpx_pdf_extract_text.py {다운로드 파일 경로}` 실행.
       - 표준 출력으로부터 **한글 텍스트 전체를 수신**.
    3. **텍스트 길이 제한 및 전처리**
       - OpenAI 토큰 한도를 고려해 **앞부분 위주로 샘플링** 또는 문단 단위로 잘라 요약에 적합한 길이로 정제.
    4. **OpenAI Chat Completion 호출**
       - 모델: `gpt-4o`
       - 프롬프트: “지원 대상 / 지원 내용 / 지원 규모 / 신청 기간 / 필수 요건 / 유의사항” 등을  
         **항목별로 bullet 형태**로 요약해 달라는 한국어 시스템 프롬프트.
    5. **응답 반환 및 (선택적) DB 저장**
       - 프론트에는 `{ success: true, summary: "..." }` 형태로 반환.
       - 주석 처리된 부분을 통해 `support_programs` 테이블의 `summary` 컬럼을 업데이트할 수 있도록 설계.

- **Frontend UX (`SupportPrograms`)**
  - 카드 단위로 “공고문 원문 요약”과 “AI 정밀 요약”을 구분해 보여줌.
    - 기본 `summary`: Bizinfo가 제공하는 요약.
    - **AI 정밀 요약**: 버튼 클릭 시 생성된 `aiSummaries[program.id]`를 bullet 리스트로 변환해서 표시.
  - 사용자는
    - 공고 제목, 주관 기관, 신청 기간, 신청 방법
    - AI가 재구성한 핵심 요약  
    을 한 화면에서 확인할 수 있어, **필터링 속도가 매우 빨라짐**.

### 3-2. 개인화된 상권 분석: 지도 상호작용 + 다중 공공데이터 결합

#### (1) 지도 인터랙션 & 반경 설정 (`AnalysisPage.tsx`)

- **기본 지도 설정**
  - Kakao Maps를 사용해 초기 중심(강남역 인근 좌표, 또는 사용자의 위치 권한 동의 시 현재 위치)으로 지도 렌더링.
  - `mapInstance`, `currentMarker`, `currentCircle` 등을 `useRef`로 관리해 **불필요한 리렌더링 없이** 지도 상태를 유지.

- **우클릭 + 드래그로 반경 설정**
  - `rightclick` 이벤트에서
    - 클릭 위치를 `centerLatLng` 로 저장하고
    - 해당 위치에 **빨간 마커 + 반경 0m짜리 원** 생성.
    - 기존 마커·원·데이터는 `clearAllData()` 로 모두 제거.
  - `mousemove` 에서는
    - `centerLatLng` 와 현재 마우스 위치로 `Polyline` 을 만들어 거리를 계산.
    - 최대 750m로 제한(`브이월드 API 안정성 고려`)하고, 그 값을 원의 반경으로 업데이트.

```tsx
// 거리 계산 및 반경 제한
const polyline = new kakao.maps.Polyline({ path: [centerLatLng.current, mouseEvent.latLng] });
let dist = Math.min(Math.round(polyline.getLength()), 750);
currentCircle.current.setRadius(dist);
setRadius(dist);
```

- **주소 및 행정동 코드 역지오코딩**
  - `updateAddress(lat, lng)` 에서 Kakao `coord2Address`, `coord2RegionCode` 를 동시에 호출하여
    - UI에 표시할 도로명/지번 주소
    - 행정동 코드(자치단체 코드)를 별도의 상태로 저장.
  - 이후 SGIS 인구 통계 및 기타 행정동 기반 API 호출에 재사용.

#### (2) 카테고리 선택 & AI 업종 매칭 (`AnalysisSidebar.tsx`)

- **3단계 업종 선택**
  - `/api/categories/large` → 대분류 리스트
  - `/api/categories/mid/:largeName` → 중분류 리스트
  - `/api/categories/small/:midName` → 소분류 리스트
  - 사용자가 차례로 버튼/셀렉트 박스를 클릭하면서 업종 계층 구조를 선택.

- **자연어 기반 업종 빠른 검색**
  - 사용자가 “커피”, “세탁소” 등 키워드를 입력하면:
    - `GET /api/categories/search?query=검색어` 호출.
    - 백엔드에서 **자연어 검색 결과를 업종 계층(hierarchy)** 로 매핑하여 반환:
      - `{ large: 대분류, mid: 중분류, small: 소분류 }`
    - `onAutoSelect()`가 이 계층 정보를 받아 **대/중/소분류를 자동으로 세팅**.

- **주소 검색 + 지도 연동**
  - `지점 위치 검색` 영역에서 Kakao Geocoder + Places API를 함께 사용.
    - 주소/지역명 + 장소(예: “명지역 카페거리”) 검색 결과를 함께 보여주고,
    - 클릭 시 `onLocationSelect(lat, lng, address)` 로 AnalysisPage에 좌표·주소 전달.
  - AnalysisPage는 해당 좌표로 지도를 이동하고, 빨간 마커/원 초기화 + 주소/행정코드 갱신.

#### (3) 데이터 수집: 시장/부동산/인구/폐업 블록

- **주요 상권 폴리곤 – `/api/market/major-districts`**
  - ODCloud 상권 API를 호출해 **유동인구·상업 집중 지역**을 폴리곤으로 가져옴.
  - 각 상권 객체에는 이름(`name`)과 경계(`path`)가 포함되어 있고,
    - 프론트에서는 Kakao Polygon으로 렌더링.
    - 마우스 오버시, CustomOverlay를 이용해 **상권 이름 태그를 띄워줌**.

- **실거래 데이터 – `/api/real-estate/trade`**
  - 국토부 RTMS 실거래 API를 통해, 최근 12개월 상업용 부동산 거래 데이터 수집.
  - 법정동 코드(`lawdCd`), 읍면동 이름(`umdNm`)을 조합해 API 호출 후,
    - 최대 12개월 * n건을 모두 합쳐 한 번에 리스트로 가공.
    - AnalysisPage에서는 Kakao Geocoder로 주소 → 좌표 변환을 거쳐,
      - 분석 반경 내에 위치한 거래만 필터링해 노란 별 마커로 지도에 표시.
    - 동시에, `recentTrades` 필드에 **요약 정보(금액/일자/지번)** 를 저장해  
      Dashboard의 “가격대 밴드” 시각화에 사용.

- **개별 공시지가 – `/api/real-estate/land-price`**
  - VWorld WFS API `getIndvdLandPriceWFS`를 사용해,  
    분석 반경을 감싸는 BBOX 안의 개별공시지가 데이터를 페이지네이션으로 모두 수집.
  - 수집된 데이터는
    - 반경 내에 있는 지점만 필터링 후
    - 가격 기준으로 하위/중간/상위 구간(Low/Mid/High)으로 나누고
    - 각 지점에 커스텀 오버레이(가격 라벨)를 표시.
  - `calculateLandPriceStats()` 함수에서는
    - 중앙부(반경/2 이내) vs 주변부(반경/2 ~ 반경) 평균 지가를 비교해  
      **입지가 “중심축인지, 변두리인지”**를 수치로 제공.

- **폐업/신규 상가 블록 – `/api/analysis/closed-blocks`**
  - 정부 개방데이터(점포 신규·폐업 인허가)를 기반으로,  
    블록(지번 단위 또는 소구역)별 **신규 개업 수(`activeCount`), 폐업 수(`closedCount`), 활력도(`vitality`)** 를 사전에 계산.
  - `/analysis/closed-blocks`에서는
    - 프론트에서 요청한 BBOX와 반경을 기준으로 해당 영역의 블록만 반환.
  - `drawClosedBlocks()` (프론트)에서는
    - `vitality`가 양수면 파란색(성장), 음수면 빨간색(위축), 0이면 회색(보합)으로 칠하고
    - `activeCount + closedCount` 에 비례해 투명도를 조절하여 **데이터 양이 많은 블록을 강조**.

- **인구 통계 – `/api/analysis/population`**
  - 주소 문자열을 입력하면:
    1. SGIS `geocode` API로 행정동 코드(`adm_cd`) 파악.
    2. 연령대·성별 조합에 대해 `searchpopulation` API를 **배치로 여러 번 호출**.
    3. 응답을 `age_eq_20`, `age_eq_30`, ..., `age_gte_70`, `male`, `female` 등의 키로 집계.
  - 프론트에서는 이를 이용해
    - 전체 인구수, **평균 연령(가중 평균)** 을 계산해 AI 리포트의 입력으로 사용.

#### (4) 데이터 가공 & AI 리포트 요청 (`/api/analysis/report`)

- **프론트 가공 단계 (AnalysisPage)**
  - 수집된 원시 데이터를 그대로 보내지 않고,  
    AI가 해석하기 좋도록 **요약·통계화된 값**만 보내는 것이 특징.
  - `summarizeShopsByDirection()`:
    - 중심 좌표 기준으로 각 상가의 **방향(북/북동/동/남동/남/남서/서/북서)** 과 거리를 계산.
    - 각 방향별 상가 수, 평균 거리 → `spatialShops` 배열.
  - `calculateLandPriceStats()`:
    - 공시지가를 정렬해 **낮은/중간/높은 가격대 비율** + 중심/주변 평균 지가 계산.
  - `computeVitalityFromBlocks()`:
    - 블록별 활력도를 이용해 **신규/폐업 상가 수 및 평균 거리** 계산.
  - 이 값들과 인구 요약, 실거래 요약을 한 번에 묶어 `/api/analysis/report` 로 전송:
    - `smallCat`, `radius`, `districts`, `landPriceStats`, `shops`, `trades`, `closures`, `population`, `spatialShops`, `blockSummary`, `address`

- **백엔드 AI 리포트 생성 (`server.js` – `/api/analysis/report`)**
  - OpenAI Chat Completion (모델: gpt-4o)을 호출하며,  
    시스템 프롬프트를 통해 **역할과 작성 원칙**을 강하게 고정:
    - “대한민국 최고의 데이터 기반 상권 분석가”로 행동.
    - “데이터가 부족하다”는 변명 금지, 가용 데이터 기반 추론 요구.
    - 공시지가를 실제 시세(1.5~2배)로 추정해 투자 가치 설명.
    - 항상 **Markdown 형식의 리포트**로 작성.
    - 신규 vs 폐업, 거리 vs 밀집도, 인구 연령 구조 등의 **해석을 별도 섹션으로 강제**.
  - User 프롬프트에는
    - 업종, 반경, 주소, 주요 상권 이름
    - 신규/실거래/폐업 수 & 평균 거리
    - 공시지가 밴드와 비교
    - 인구 구조와 타깃 적합성
    - 방향별 상가 밀집도(spatialShops)
    - 블록 단위 신규/폐업 요약  
    이 모두 포함됨.
  - 응답으로 돌아온 Markdown 리포트는 `setAnalysisResult({ aiReport: data.report })` 를 통해 Zustand에 저장되고,
    - `AnalysisDashboard`에서 ReactMarkdown으로 그대로 렌더링.

### 3-3. 데이터 연속성: 분석 페이지 ↔ 추천 페이지 상태 유지

#### (1) 전역 스토어 구조 (`useAnalysisStore.ts`)

- **핵심 필드**
  - **입력 컨텍스트**
    - `coords`: 분석 중심 좌표 `{ lat, lng }`
    - `radius`: 분석 반경 (m)
    - `address`: 선택된 주소 문자열
    - `selectedCategory`: `{ large, mid, small }` – 대/중/소분류 업종
    - `locationConsent`: `'allowed' | 'denied' | 'unknown'` – 위치 권한 상태
  - **분석 결과 데이터**
    - `population`: 평균 연령/전체 인구
    - `shops`: 반경 내 상가 리스트
    - `closedShops`: 폐업 상가 원시 데이터
    - `landPrices`: 공시지가 데이터
    - `majorDistricts`: 주요 상권 폴리곤 리스트
    - `recentTrades`: 실거래 요약
    - `blocks`: 블록 단위 신규/폐업/활력도 + geometry + center
    - `aiReport`: 상권 분석 AI 리포트 (Markdown 문자열)
    - `supportPrograms`: 지역 맞춤 지원사업 리스트

- **메서드**
  - `setAnalysisResult(data: Partial<AnalysisData>)`  
    → 기존 상태에 **부분 업데이트**를 적용하는 통합 setter.
  - `setLocationConsent(status)`  
    → 위치 권한 플로우 제어.
  - `resetAnalysis()`  
    → 반경 및 분석 결과를 초기화하여 새 분석 시작.

#### (2) 분석 결과 복원 & 페이지 간 연동

- **AnalysisPage 진입 시 복원 로직**
  - `hasHydratedFromStore` 플래그를 이용해:
    - 스토어에 이전 분석의 `radius`, `coords`, `address`, `selectedCategory` 가 남아있으면
      - 지도 중심과 마커, 반경 원, 사이드바 선택 값, Dashboard 열림 상태를 한 번에 복원.
  - Land price, shops, majorDistricts, blocks가 이미 스토어에 있으면
    - 지도 위 마커/폴리곤을 재생성하여 **새로고침 후에도 동일 화면 유지**.

- **RecommendationPage에서의 사용**
  - `useAnalysisStore()`로
    - `coords`, `radius`, `address`, `selectedCategory`, `blocks`를 그대로 가져와 **후처리**.
  - 따로 API를 다시 호출하지 않고,  
    **분석 페이지에서 계산한 블록 데이터만으로 추천 알고리즘을 수행**.

### 3-4. 사용자 맞춤형 상권 추천 및 후보 비교 (`RecommendationPage.tsx`)

#### (1) 블록 스코어링 알고리즘

- **입력 데이터**
  - `blocks`: 각 블록은 다음 정보를 가짐
    - `properties.activeCount` – 신규(영업 중) 상가 수
    - `properties.closedCount` – 폐업 상가 수
    - `properties.vitality` – 활력도 지표(신규·폐업을 종합한 값)
    - `center.lat`, `center.lng` – 블록 중심 좌표

- **점수 산정**
  - `vitalityScore`: `(vitalityRaw + 1) * 40` (최대 80점)
  - `densityScore`: `min(activeCount * 5, 20)` (최대 20점)
  - `closureRatio`: `closed / (active + closed)` – 폐업 비율
  - 시나리오별 가중치:
    - **균형 추천(balanced)**: `totalScore = vitalityScore + densityScore`
    - **안정 우선(safe)**:  
      `totalScore = vitalityScore * 0.6 + densityScore * 0.2 - closureRatio * 80`
    - **성장 우선(growth)**:  
      `totalScore = vitalityScore * 1.0 + densityScore * 0.4 - closureRatio * 30`
  - 상태 태그
    - `statusTag`: `성장형 상권 / 보합 상권 / 위축 상권` 을 vitality 기준으로 분류.
    - `riskTag`: 폐업 비율에 따라 `폐업 리스크 낮음/보통/높음` 으로 구분.
  - 상위 N개(`recommendCount` 슬라이더로 3~20개)를 **추천 후보 리스트**로 선정.

#### (2) 지도 연동 & 후보 선택 UX

- 왼쪽 패널
  - RANK, 총점, 주소(좌표 기반 지오코딩), 상태 태그, 신규/폐업·폐업 비율이 한 카드에 표시.
  - 각 후보 옆의 체크박스로 **최대 3개까지 비교 대상**으로 선택 가능.
  - 카드 클릭 시
    - 해당 블록 중심으로 지도가 이동하고
    - 블록 경계를 파란 폴리곤으로 강조.

- 지도 상 오버레이
  - 비교 대상으로 선택된 후보는 **연두색 폴리곤**으로 표시.
  - 현재 선택된 후보와 비교 후보의 색상/레이어를 분리해  
    **하나의 지도에서 “선택된 곳 vs 후보들”을 직관적으로 비교**할 수 있게 함.

#### (3) AI 기반 리포트: 단일 블록 vs 복수 후보 비교

- **상세 블록 리포트 – `/api/analysis/block-report`**
  - 선택된 블록의
    - 신규/폐업 수, 활력도, 폐업 비율, 총점, 주소 등을 백엔드에 전송.
  - 백엔드에서 OpenAI를 호출해
    - 해당 블록의 입지 매력도, 리스크, 추천 전략(보수적/공격적), 적합 업종 포지셔닝 등을  
      Markdown 형식으로 리포트화.
  - RecommendationPage의 하단/우측 패널에서 ReactMarkdown으로 표시.

- **후보 비교 리포트 – `/api/analysis/compare-blocks`**
  - 사용자가 선택한 최대 3개 블록에 대해
    - `후보 1/2/3` 라벨, 주소, 신규/폐업 수, 활력도, 폐업 비율, 점수 등을 배열로 전달.
  - 백엔드는
    - “각 후보의 상대적인 장단점, 어떤 창업 성향(안정/성장/실험)에 어떤 블록이 맞는지”를  
      텍스트로 풀어주는 비교 리포트를 생성.
  - 비교 리포트 패널에서는 탭 전환(`이 구역 리포트 / 비교 리포트`)을 통해  
    **단일 후보 분석과 후보 간 비교를 한 화면에서 전환**할 수 있음.

---

## 4. 시스템 아키텍처

### 4-1. 전체 구조 개요

```text
[사용자 브라우저]
   ├─ React SPA
   │   ├─ App (Router)
   │   │   ├─ /analysis → AnalysisPage
   │   │   └─ /recommendation → RecommendationPage
   │   ├─ Header (상단 내비게이션)
   │   ├─ AnalysisPage
   │   │   ├─ AnalysisSidebar (업종·주소·반경 입력)
   │   │   ├─ Kakao Map (지도 및 마커/폴리곤)
   │   │   ├─ MapControls (레이어 토글)
   │   │   └─ AnalysisDashboard (AI 리포트 & 지원사업)
   │   └─ RecommendationPage (후보 블록 추천·비교)
   │
   └─ Zustand Store (useAnalysisStore)
         ├─ 분석 컨텍스트 (coords, radius, category, address)
         ├─ 상권 데이터 (shops, landPrices, majorDistricts, blocks, recentTrades)
         └─ 리포트 데이터 (aiReport, supportPrograms, population)

[Backend – Node.js/Express (server.js)]
   ├─ 도메인 API
   │   ├─ /api/categories/*
   │   ├─ /api/market/major-districts
   │   ├─ /api/market/search (상가 검색)
   │   ├─ /api/real-estate/trade
   │   ├─ /api/real-estate/land-price
   │   ├─ /api/analysis/closed-blocks
   │   ├─ /api/analysis/population
   │   ├─ /api/analysis/report
   │   ├─ /api/analysis/block-report
   │   ├─ /api/analysis/compare-blocks
   │   ├─ /api/get-support
   │   └─ /api/support/summarize
   │
   └─ 외부 서비스 연동
       ├─ MySQL (카테고리, 내부 캐시 등)
       ├─ Bizinfo API (지원사업 목록/첨부파일)
       ├─ 국토부 RTMS API (실거래)
       ├─ VWorld WFS API (개별공시지가)
       ├─ SGIS API (인구 통계)
       ├─ 공공데이터포털 상권 API (주요 상권 폴리곤)
       ├─ OpenAI Chat Completion API (AI 리포트/요약)
       └─ Python 스크립트 (HWP/HWPX/PDF 텍스트 추출)
```

### 4-2. 주요 데이터 플로우

- **상권 분석 플로우**
  1. 사용자가 AnalysisPage에서 **주소/업종/반경** 설정 후 “데이터 분석 시작” 클릭.
  2. Frontend는 Kakao 지도/지오코딩을 통해 중심 좌표·행정동 코드 등을 확정.
  3. Frontend → Backend REST 호출 (병렬):
     - `/market/major-districts`, `/real-estate/land-price`, `/real-estate/trade`, `/market/search`, `/analysis/closed-blocks`, `/analysis/population`
  4. Backend는 각 공공 API를 호출 후, 필요한 필드만 남겨서 JSON으로 반환.
  5. Frontend는 응답들을 **요약 통계와 공간 지표**(방향/거리, 밴드, 비율 등)로 가공.
  6. 가공된 요약 데이터를 `/analysis/report` 로 전송 → OpenAI가 Markdown 리포트 생성.
  7. 리포트와 원시 데이터(shops, blocks, landPrices, recentTrades 등)를 Zustand 스토어에 저장.
  8. AnalysisDashboard와 지도/대시보드 UI에서 이를 시각화.

- **지원사업 분석 플로우**
  1. AnalysisPage에서 `fetchSupportPrograms()` 호출로 `/api/get-support` 요청.
  2. Backend는 Bizinfo API에서 **해당 지역 소상공인 지원사업**만 필터링해 반환.
  3. SupportPrograms 컴포넌트가 지원사업 리스트를 노출.
  4. 사용자가 특정 공고의 “AI로 공고문 요약하기” 클릭:
     - `/api/support/summarize` 로 첨부파일 URL 전송.
     - Node.js가 공고문 파일을 다운로드 → Python 스크립트로 텍스트 추출 → OpenAI에 요약 요청.
  5. 생성된 요약 텍스트를 해당 카드에 즉시 반영.

- **추천 입지 플로우**
  1. 사용자가 `/recommendation` 페이지로 이동.
  2. RecommendationPage는 `useAnalysisStore()`에서 **blocks, coords, radius, category**를 그대로 읽어옴.
  3. 각 블록에 대해 시나리오별 점수를 계산해 상위 N개 후보를 선정.
  4. 사용자가 후보를 선택하면 지도에 폴리곤/오버레이로 강조.
  5. 필요 시 `/analysis/block-report`, `/analysis/compare-blocks` 를 호출하여  
     **후보별·후보간 AI 리포트**를 생성하고 패널에 표시.

---

## 5. 핵심 구현 로직 (Code Highlights)

### 5-1. Kakao 지도 기반 반경 선택 UX

- **아이디어**
  - 단순히 “반경 숫자 입력”이 아니라,  
    **지도에서 우클릭 후 드래그만으로 분석 범위를 직관적으로 지정**.

- **로직 요약**

```tsx
// 우클릭으로 중심 지정 및 기존 데이터 초기화
kakao.maps.event.addListener(map, 'rightclick', (mouseEvent) => {
  const clickLatLng = mouseEvent.latLng;
  centerLatLng.current = clickLatLng;
  map.panTo(clickLatLng);
  updateAddress(clickLatLng.getLat(), clickLatLng.getLng());
  clearAllData();
  // 빨간 마커 + 반경 0m 원 생성
});

// 드래그로 반경 조정
kakao.maps.event.addListener(map, 'mousemove', (mouseEvent) => {
  if (!isDragging.current || !currentCircle.current) return;
  const polyline = new kakao.maps.Polyline({ path: [centerLatLng.current, mouseEvent.latLng] });
  const dist = Math.min(Math.round(polyline.getLength()), 750);
  currentCircle.current.setRadius(dist);
  setRadius(dist);
});
```

### 5-2. 방향·거리 기반 상가 요약 (Spatial Aggregation)

- **목적**
  - 단순히 “반경 내 상가 수”가 아니라,  
    **어느 방향에 얼마나 몰려 있는지**를 AI가 이해할 수 있도록 요약.

- **로직 요약**

```ts
const summarizeShopsByDirection = (shops, centerLat, centerLng, maxRadius) => {
  const dirLabels = ['북', '북동', '동', '남동', '남', '남서', '서', '북서'];
  const buckets = {}; // direction → { count, totalDistance }

  shops.forEach(shop => {
    const lat = Number(shop.lat);
    const lng = Number(shop.lon ?? shop.lng);
    const dLat = (lat - centerLat) * 111000;
    const dLng = (lng - centerLng) * 111000 * Math.cos(centerLat * Math.PI / 180);
    const dist = Math.sqrt(dLat * dLat + dLng * dLng);
    if (dist > maxRadius) return;

    const angle = (Math.atan2(dLng, dLat) * 180) / Math.PI; // 0° = 북
    const idx = Math.round(((angle + 360) % 360) / 45) % 8;
    const key = dirLabels[idx];

    if (!buckets[key]) buckets[key] = { count: 0, totalDistance: 0 };
    buckets[key].count += 1;
    buckets[key].totalDistance += dist;
  });

  return Object.values(buckets).map(b => ({
    direction: b.direction,
    count: b.count,
    avgDistance: b.count ? Math.round(b.totalDistance / b.count) : 0,
  }));
};
```

### 5-3. 공시지가 밴드 & 핵심축 vs 주변부 분석

- **목적**
  - 단순 평균이 아닌, **저가/중가/고가 구간과 중심부/주변부의 가격 차이**를 모두 반영해  
    “이 위치가 상권의 중심축인지, 외곽인지”를 판단.

- **핵심 아이디어**
  - 유효한 공시지가만 필터링 후, 가격 기준으로 정렬.
  - 하위 1/3, 중간 1/3, 상위 1/3 구간을 나눠 비율 집계.
  - 중심에서의 거리 기준으로 반경/2 이내 vs 그 밖의 평균 지가를 계산.

### 5-4. 블록 단위 활력도 시각화 & 추천 알고리즘

- **활력도 시각화**
  - 각 블록의 `activeCount`, `closedCount`, `vitality` 를 활용해
    - 색상: 파랑(성장) / 빨강(위축) / 회색(보합)
    - 투명도: `(active + closed)` 에 비례하게 설정 → **데이터가 많은 블록을 강조**.
  - 클릭 시 InfoWindow에 지번, 신규/폐업 수, 상태 등을 표시.

- **추천 점수 계산**
  - 활성도·밀집도·폐업 비율을 조합해 **시나리오별(totalScore)** 을 산출.
  - RecommendationPage에서는 이 점수를 기준으로 TOP N 후보를 선정하고,  
    지도와 카드 UI를 통해 **정량·정성 정보를 동시에** 제공.

### 5-5. AI 리포트 일관성 확보를 위한 Prompt 설계

- **공통 전략**
  - 모든 AI 호출에서 **Markdown 형식**을 강제하여,
    - 프론트엔드에서 추가 파싱 없이 ReactMarkdown으로 바로 렌더링 가능.
  - “데이터 부족을 이유로 회피 금지”, “가용 데이터 최대 활용”을 반복적으로 명시.
  - 상권 분석 시에는
    - “입지 가치”, “경쟁 구조”, “실거래 가격대”, “공식 상권 겹침”, “신규/폐업 흐름”, “인구·타깃 적합성”  
      등 서브 섹션을 안내해 **항목별 일관성 있는 리포트**를 생산.

---

## 6. 마무리

- **SBC 365**는 단순히 상권 통계나 지원사업 리스트를 보여주는 수준을 넘어,
  - **공고문 자동 분석(문서 레벨)** 과  
  - **사용자 맞춤형 AI 상권 추천(공간 데이터 레벨)** 을 결합한 **실행 중심 도구**입니다.
- React + Node.js + Python + OpenAI + 공공 데이터 API들을 유기적으로 결합해,
  - “지도에서 한 번 분석하면, 그 데이터를 그대로 활용해 후보 입지를 비교·결정”할 수 있는 **연속된 사용자 여정**을 구현했습니다.
- 향후 발전 방향으로는
  - 상권 변화의 시계열 추세(연도별 활력도), 실시간 카드 매출 데이터, 점포 실제 매물 정보 등과의 연동을 통해  
    **더 정교한 수익성 예측과 리스크 시뮬레이션**까지 확장할 수 있습니다.

